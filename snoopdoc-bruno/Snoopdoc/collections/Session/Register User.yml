info:
  name: Register User
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/auth/register"
  body:
    type: json
    data: |-
      {
        "firstName": "El-J",
        "lastName": "Diaz",
        "email": "eljdiaz500@gmail.com",
        "password": "Password123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
