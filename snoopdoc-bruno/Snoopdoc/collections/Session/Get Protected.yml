info:
  name: Get Protected
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/auth/protected"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
